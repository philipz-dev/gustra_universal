/**
 * Same review detail as Reviews stack, scoped under My Gustra so Back returns
 * to the passport (Top-3 review → My Gustra), not the Reviews feed.
 */
export { default } from '../../(main)/review/[id]';
